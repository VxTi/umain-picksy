{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "fs:read-files",
    "fs:scope-home",
    "fs:allow-exists",
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:default",
    "store:default"
  ]
}
